services:
  app:
    build: .
    container_name: voicesrt
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
